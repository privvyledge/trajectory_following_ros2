These files contain my original implementations before the major refactor.
I have kept them for legacy reasons since they include a lot of useful comments and commented out equivalent alternative methods/implementations.
The major issue with this version was that all controller nodes duplicated the same boilerplate and as such bugs needed to be fixed multiple times.